     #categories_index = api_client.wow.game_data.get_achievement_categories_index("us", "en_US")
        #categories_index_json_object = json.loads( categories_index)
        #creatures_family_extract = json.loads(creatures_family_index)
        #creatures_family_index_formatted = json.dumps(creatures_family_index,indent=2)
        #categories_index_formatted = json.dumps(categories_index,indent=2)
         #print(creatures_family_index_formatted)
        #print(categories_index_formatted)
   
   
   
   printing out value in the arrays
     #for id, name in extracted_data:
            #print(f"ID: {id}, Name: {name}")
       #for id in extracted_ids:  save for debugging
            #print(id)   
        #for creature_name in creatures_family_index['creature_families']:
            #extracted_names.append(creature_name['name'])   
        #for name in extracted_names:
           # print(name)